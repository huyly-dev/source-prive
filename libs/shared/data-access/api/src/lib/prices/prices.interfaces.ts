export interface IPriceListItem {
  instrument: string;
  base: string;
  counter: string;
  price: number;
}

export interface IPriceListItemVM {
  instrument: string;
  base: string;
  counter: string;
  price: number;
}
