import { AlertSectionIconPipe } from './section-icon.pipe';

describe('AlertSectionIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertSectionIconPipe();
    expect(pipe).toBeTruthy();
  });
});
