import { AlertInlineIconPipe } from './inline-icon.pipe';

describe('AlertInlineIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertInlineIconPipe();
    expect(pipe).toBeTruthy();
  });
});
