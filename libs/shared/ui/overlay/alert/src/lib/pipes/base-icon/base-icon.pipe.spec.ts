import { AlertBaseIconPipe } from './base-icon.pipe';

describe('AlertBaseIconPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertBaseIconPipe();
    expect(pipe).toBeTruthy();
  });
});
