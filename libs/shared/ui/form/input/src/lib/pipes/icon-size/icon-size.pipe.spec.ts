import { PriveFormInputIconSizePipe } from './icon-size.pipe';

describe('PriveFormInputIconSizePipe', () => {
  it('create an instance', () => {
    const pipe = new PriveFormInputIconSizePipe();
    expect(pipe).toBeTruthy();
  });
});
