import { DropdownSelectedPipe } from './selected.pipe';

describe('DropdownSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
