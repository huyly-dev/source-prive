import { TableEmptyCustomDirective } from './table-empty-custom.directive';

describe('TableEmptyCustomDirective', () => {
  it('should create an instance', () => {
    const directive = new TableEmptyCustomDirective();
    expect(directive).toBeTruthy();
  });
});
