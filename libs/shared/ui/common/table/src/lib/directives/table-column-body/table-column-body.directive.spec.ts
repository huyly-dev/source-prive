import { TableColumnBodyDirective } from './table-column-body.directive';

describe('TableColumnBodyDirective', () => {
  it('should create an instance', () => {
    const directive = new TableColumnBodyDirective();
    expect(directive).toBeTruthy();
  });
});
